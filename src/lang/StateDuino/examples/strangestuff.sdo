StateMachine StrangeStuff(int a, int b, bool c)
start = StartState

// shadowing both forks and actions
// do we really want this?
StartState => NestedFork? {
	yes => StartState => NestedFork? {
		yes => StartState => NestedFork?
	}
}

// just some nested definitions of Forks and Actions
A1 => A2 => T1?
T1? {
	yes => A1 => !T2? {
		yes => A1
		no => T1? {
			yes => A1 => T1? {
				yes => A1
			}
		}
	}
}

// this shouldn't be possible, A1 is already declared
A1 => !T2? {
	yes => T1?
}